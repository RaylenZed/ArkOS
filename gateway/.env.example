# Gateway stack env
TZ=Asia/Shanghai

# Domain + TLS
BASE_DOMAIN=pve.example.com
ACME_EMAIL=you@example.com
CF_DNS_API_TOKEN=replace-with-cloudflare-token

# Public HTTPS ports
EMBY_HTTPS_PORT=48443
QBIT_HTTPS_PORT=42053
OPENLIST_HTTPS_PORT=42096
DIFY_HTTPS_PORT=43053
# Dify upstream service target on shared Docker network
# Keep default unless Dify service name changes in future upgrades
DIFY_UPSTREAM=nginx:80

# Data path
CADDY_DATA=/srv/docker/caddy/data
CADDY_CONFIG=/srv/docker/caddy/config

# Shared external docker network (must match other stacks)
ARK_NETWORK=arkos-net
