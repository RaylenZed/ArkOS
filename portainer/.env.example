# Portainer stack env
TZ=Asia/Shanghai

PORTAINER_BIND_IP=127.0.0.1
PORTAINER_HTTPS_PORT=49443
PORTAINER_DATA=/srv/docker/portainer

# Shared external docker network (must match other stacks)
ARK_NETWORK=arkos-net
